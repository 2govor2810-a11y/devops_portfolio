version: '3.8'

services:
  fastapi:
    build: .  # Собираем из Dockerfile
    ports:
      - "8000:8000"  # Хост:Контейнер
    volumes:
      - ./app:/app  # Для разработки: монтируем код
    environment:
      - ENVIRONMENT=development
      - DEBUG=true
    restart: unless-stopped